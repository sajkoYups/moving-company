# Quote form: recipient address (used when sending email)
QUOTE_EMAIL_TO=quote@example.com

# Optional: Resend API key and from address for sending quote emails. If not set, quote requests are logged only.
# RESEND_API_KEY=re_xxxx
# RESEND_FROM_EMAIL=quote@yourdomain.com

# Optional: Google Analytics / Ads (Consent Mode v2). Cookie banner updates consent.
# NEXT_PUBLIC_GA_ID=G-xxxx

# Site URL for SEO (canonical URLs, sitemap, Open Graph). Required for production.
# NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# Optional: Phone number displayed in header and contact sections
# NEXT_PUBLIC_PHONE=+49 40 12345678
